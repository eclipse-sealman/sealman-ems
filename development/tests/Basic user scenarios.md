# Basic user scenarios

Basic user scenarios should mimic basic user interactions with the system. They should connect multiple endpoints together and focus on simple, most common and probably success scenarios.

Located in:

```bash
tests/WebApi/Scenario/
```
